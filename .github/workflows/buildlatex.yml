      - name: Find and compile all LaTeX documents
        run: |
          for dir in $(find . -maxdepth 1 -type d -name "week_*"); do
            if [[ -f "$dir/main.tex" ]]; then
              echo "🔍 Found main.tex in $dir"
              docker run --rm -v "$PWD/$dir:/work" -w /work --user root texlive/texlive bash -c "
                apt-get update && apt-get install -y fonts-liberation fontconfig && \
                mkdir -p /usr/share/fonts/truetype/msttcore && \
                cd /usr/share/fonts/truetype/msttcore && \
                curl -O https://archlinux.org/packages/extra/any/ttf-ms-fonts/download && \
                fc-cache -fv && \
                luaotfload-tool --update && \
                latexmk -lualatex -interaction=nonstopmode main.tex"
              mv "$dir/main.pdf" "$dir/weekly_meeting.pdf"
            else
              echo "⚠️ No main.tex found in $dir, skipping."
            fi
          done
