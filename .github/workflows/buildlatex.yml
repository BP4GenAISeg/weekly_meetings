name: Build LaTeX documents
on: 
  push:
    paths-ignore:
      - "**/weekly_meeting.pdf"
  schedule:
    - cron: '0 22 * * *'

jobs:
  build_latex:
    runs-on: ubuntu-latest
    steps:
      - name: Set up Git repository
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.TOKEN }}

# ... keep the top parts unchanged ...

# ... keep previous workflow sections unchanged ...

      - name: Find and compile all LaTeX documents
        run: |
          for dir in $(find . -maxdepth 1 -type d -name "week_*"); do
            if [[ -f "$dir/main.tex" ]]; then
              echo "🔍 Found main.tex in $dir"
              abs_path=$(realpath "$dir")
              docker run --rm -v "$abs_path:/work" -w /work --user root texlive/texlive bash -c "
                echo '🔄 Comprehensive font setup...'
                # Install essential dependencies
                apt-get update -qq && apt-get install -y \
                    wget \
                    unzip \
                    fontconfig \
                    libfreetype6 \
                    lmodern

                # Create TeX-specific font directory
                mkdir -p /usr/local/texlive/texmf-local/fonts/truetype/arial
                
                # Download and install Arial directly to TeX tree
                wget -q -O /tmp/arial.zip https://github.com/rednoah/ttf-font-arial/archive/master.zip
                unzip -q /tmp/arial.zip -d /tmp/
                find /tmp/ttf-font-arial-master/ -name '*.ttf' -exec cp {} /usr/local/texlive/texmf-local/fonts/truetype/arial/ \;
                
                # Update TeX font databases
                mktexlsr
                updmap-sys --force --enable Map=arial.map
                luaotfload-tool --update --force
                fc-cache -fv

                # Configure environment variables
                export OSFONTDIR=/usr/local/texlive/texmf-local/fonts/:/usr/local/share/fonts/
                
                # Verify installation
                echo '=== System font list ==='
                fc-list | grep -i arial
                echo '=== TeX font map ==='
                luaotfload-tool --find='Arial'
                
                echo '🛠️ Compiling document...'
                latexmk -lualatex -interaction=nonstopmode main.tex"
              mv "$dir/main.pdf" "$dir/weekly_meeting.pdf"
            else
              echo "⚠️ No main.tex found in $dir, skipping."
            fi
          done

      - name: Upload PDFs
        uses: actions/upload-artifact@v4
        with:
          name: Weekly Meetings PDFs
          path: "**/weekly_meeting.pdf"

      - name: Commit and push PDFs
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add -f **/weekly_meeting.pdf
          git commit -m "📄✨ docs: automatic update of weekly meeting documents ✨📄" || echo "No changes to commit"
          git push || echo "No changes to push"
